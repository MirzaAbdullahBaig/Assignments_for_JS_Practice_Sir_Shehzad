let heading = `Rules for naming JS Variable`;

document.write(`${heading} <br/> <br/> <br/>`);

let var_name = "Variable name can only contain letters, numbers, underscores and dollar. For example: $my_1stVariable";

let var_name_begin = "Variable must begin with a letter, underscore and dollar. For example name, $name and _name";

let var_rule_1 = "Variable names are case-insensitive"
let var_rule_2 = "Variable names should not be JS keywords"

document.write(`${var_name} <br/> ${var_name_begin} <br/> ${var_rule_1} <br/> ${var_rule_2}`)